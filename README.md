Super simple file watcher

Usage: ```watcher --folder PATH [--on-add COMMAND] [--on-modify COMMAND] [--on-remove COMMAND]```

Note that script files (such as .sh, or .py) must contain a shebang on Linux, otherwise the OS will attempt to start a binary.
